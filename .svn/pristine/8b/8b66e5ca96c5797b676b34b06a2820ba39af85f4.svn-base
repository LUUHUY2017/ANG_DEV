import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { AgeVisitComponent } from './age-visit.component';

describe('FbaMetricsComparisonComponent', () => {
  let component: AgeVisitComponent;
  let fixture: ComponentFixture<AgeVisitComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ AgeVisitComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(AgeVisitComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
