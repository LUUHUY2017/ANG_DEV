import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { AgeMetricsComparisonComponent } from './age-metrics-comparison.component';

describe('FbaMetricsComparisonComponent', () => {
  let component: AgeMetricsComparisonComponent;
  let fixture: ComponentFixture<AgeMetricsComparisonComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ AgeMetricsComparisonComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(AgeMetricsComparisonComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
